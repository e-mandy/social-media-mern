import { Outlet } from 'react-router'
import { useEffect } from 'react'

const AppLayout = () => {

  useEffect( () => {
    
  })

  return (
    <div>
      <Outlet />
    </div>
  )
}

export default AppLayout
